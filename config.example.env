# Example environment variables for the reminder container
HOMEBOX_API_BASE=https://demo.homebox.software/api
# If your Homebox instance requires a token, set it here
HOMEBOX_API_TOKEN=
# Seconds between checks (default 3600)
CHECK_INTERVAL_SECONDS=3600
# Optional webhook URL to POST notifications to (e.g., IFTTT, Slack incoming webhook, or generic receiver)
# Apprise supports many providers. Provide a comma-separated list of apprise URLs.
# Example (Discord webhook): discord://webhook_id/webhook_token
NOTIFIER_URLS=
# Backwards-compatible single webhook (JSON POST payload {"text": "..."})
NOTIFIER_WEBHOOK=

# Reminder timing
# How many days before the scheduled date to start reminding (integer >= 0)
REMIND_START_DAYS_BEFORE=0
# How many days after the scheduled date to continue reminding (integer >= 0)
REMIND_END_DAYS_AFTER=0
# Repeat interval in days. 0 = only once on the start date. >0 = repeat every N days.
REMIND_REPEAT_DAYS=0

# OIDC / Token options
# If you use OIDC and can't perform the flow from this container, you can obtain a
# session/token from the Homebox web UI (look for the `hb.auth.session` cookie or
# the API login response) and provide it here.
# Option 1: set the raw token directly
HOMEBOX_OIDC_TOKEN=
# Option 2: mount a file with the token and point to it
HOMEBOX_TOKEN_FILE=/data/homebox_token.txt

# Persistence DB path (container path). Mount a host volume at /data to persist state between runs.
REMINDERS_DB=/data/reminders.db
# How many days to keep reminder records. 0 disables automatic pruning at startup.
REMIND_RETENTION_DAYS=0
# Set to true to run a single check and exit (useful for testing)
RUN_ONCE=false
